apiVersion: v1
kind: Service
metadata:
  name: parselab
  namespace: oasis-be
spec:
  type: ClusterIP
  selector:
    app: parselab
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
