apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oasis-be

resources:
  - configmap.yaml
  - deployment.yaml
  - ingress.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: parselab
      app.kubernetes.io/part-of: parselab
